# --- core pipeline ---
datasets
sentence-transformers
qdrant-client
faker
pandas
numpy
torch
python-dotenv

# --- agent loop + local LLM (Ollama) ---
# Pinned to the 0.3-era LangChain: ragas 0.4.3 is incompatible with LangChain 1.x
# (it imports langchain_community.chat_models.vertexai, removed in community 0.4).
langgraph>=0.2,<0.3
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-ollama>=0.2,<0.3
ollama

# --- governance controls ---
presidio-analyzer
presidio-anonymizer

# --- evaluation ---
matplotlib
ragas==0.4.3          # local-judge faithfulness (default backend); needs LangChain 0.3.x above
scikit-network        # ragas dependency; needs a C/Cython (MSVC) toolchain to build on Windows
# NLI entailment fallback (sentence-transformers, already above) is used automatically
# if RAGAS ever errors at runtime — set evaluation.faithfulness_backend: nli to force it.

# --- misc ---
pyyaml
tqdm